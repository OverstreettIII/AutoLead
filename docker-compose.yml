services:
  web:
    build: ./backend
    container_name: autolead_web
    command: python manage.py runserver 0.0.0.0:8001
    volumes:
      - ./backend:/app
    working_dir: /app
    ports:
      - "8001:8001"
    env_file:
      - .env
